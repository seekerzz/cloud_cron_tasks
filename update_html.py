import re

with open("DEST_REPO/arxiv-daily/index.html", "r", encoding="utf-8") as f:
    content = f.read()

summaries = [
    """<ul>
<li><strong>核心贡献总结：</strong>
<ul>
<li>针对音频深度伪造检测，首次对监督对比学习（SupCon）进行了专门的对照研究。</li>
<li>在wav2vec2 XLS-R模型上，通过改变SupCon中的相似度度量（余弦相似度与角度相似度）和负样本缩放策略（使用全局跨批次队列）进行了深入分析。</li>
<li>实验表明，结合延迟队列的余弦SupCon取得了最佳的检测性能（ITW EER达到8.29%，汇总EER达到4.44%），而角度相似度在不使用队列的情况下表现依然强劲，减少了对大量负样本的依赖。</li>
</ul></li>
</ul>""",
    """<ul>
<li><strong>核心贡献总结：</strong>
<ul>
<li>针对科学交流等特定领域，评估了多种先进的语音克隆模型在阿拉伯语、中文和法语科学文本的跨语言语音生成表现。</li>
<li>基于OmniVoice基础模型构建了语音克隆系统，参与IWSLT 2026跨语言语音克隆共享任务。</li>
<li>引入了来自ACL 60/60语料库的多模型集成蒸馏技术进行数据增强，实验证明使用合成数据微调能够一致地提升跨语言生成语音的清晰度（降低WER和CER），同时有效保留说话人的音色特征。</li>
</ul></li>
</ul>""",
    """<ul>
<li><strong>核心贡献总结：</strong>
<ul>
<li>提出了一种基于非线性动力学的数字生物标志物，通过分析对话语音动态中的复发结构（即语音系统随时间重访声学状态的模式）来识别抑郁症。</li>
<li>在包含142名参与者的DAIC-WOZ语料库抑郁子集上，从74个语音通道提取基于复发的生物标志物，构建了逻辑回归分类模型。</li>
<li>实验结果显示，该方法实现了0.689的平均交叉验证AUC，显著优于传统的静态声学基线以及其他非线性特征，证明了非线性状态空间分析在精神医学数字标志物研究中的潜力。</li>
</ul></li>
</ul>""",
    """<ul>
<li><strong>核心贡献总结：</strong>
<ul>
<li>提出了一种名为DiffAnon的基于扩散模型的语音匿名化方法，结合无分类器引导（CFG）技术，解决了现有系统在效用和隐私之间固定权衡的问题。</li>
<li>DiffAnon在RVQ编解码器的语义嵌入上优化声学细节，允许在单一模型中对匿名化强度和韵律保真度进行平滑插值。</li>
<li>这是首个在推理阶段提供结构化、可插值韵律控制的语音匿名化框架，实验表明其在实现强大效用的同时，能够在不同操作点保持极具竞争力的隐私保护效果。</li>
</ul></li>
</ul>""",
    """<ul>
<li><strong>核心贡献总结：</strong>
<ul>
<li>系统研究了在极低码率下，引入冻结语义先验（如HuBERT和Whisper）对神经语音编码的作用及其局限性。</li>
<li>发现并验证了“语义衰退”现象（Semantic Retirement），即在1.5 kbps等极低码率下语义约束能显著降低词错误率，但在超过6 kbps后其收益会迅速减少。</li>
<li>揭示了不同类型先验之间的权衡：声学丰富的先验（HuBERT）更善于保留韵律和音色细节，而高层语言先验（Whisper）则能有效抑制噪声环境下的语音幻觉（降低26%）并提升对未见说话人的泛化能力。</li>
<li>提出了一种感知码率的调节策略，能够动态调整先验强度以优化语义一致性与感知自然度，为极低码率生成式语音编码提供了一条原则性路径。</li>
</ul></li>
</ul>""",
    """<ul>
<li><strong>核心贡献总结：</strong>
<ul>
<li>针对跨语言说话人验证中严重的“语言-说话人纠缠”问题，指出传统的盲目对抗解耦方法会错误惩罚与语言相关的说话人判别特征。</li>
<li>提出了一种Dual-LoRA架构，通过在冻结的预训练主干网络中注入可训练的、按任务分解的LoRA适配器来解耦特征。</li>
<li>核心创新在于引入“语言锚定对抗（Language-Anchored Adversary）”，利用明确的语言分支引导判别器，使对抗梯度精确针对真实语言线索，从而保留说话人的核心特征。</li>
<li>在TidyVoice基准测试中表现卓越，验证集等错误率（EER）达到0.91%，并在官方挑战赛中荣获第三名。</li>
</ul></li>
</ul>""",
    """<ul>
<li><strong>核心贡献总结：</strong>
<ul>
<li>对语音生成领域当前广泛依赖的“基于深度嵌入（如emotion2vec）余弦相似度的情感相似度评估方法”提出了质疑。</li>
<li>通过控制性的对抗任务和人类对齐测试发现，尽管这些编码器在分类任务上具有高准确率，但其潜在空间并不适合用于零样本相似度评估。</li>
<li>揭示了现有表示方法的局限性：语言和说话人特征的干扰往往会掩盖情感特征，导致客观指标严重偏离人类实际感知。</li>
<li>证明了现有评估指标存在声学脆弱性，更倾向于奖励表面上的声学模仿，而非真正的情感合成。</li>
</ul></li>
</ul>""",
    """<ul>
<li><strong>核心贡献总结：</strong>
<ul>
<li>首次在大规模可部署的层面上，将基于音频的口吃系统从“检测”（当前是否存在）拓展到“预测”（即将发生什么），为闭环干预提供了技术基础。</li>
<li>训练了一个轻量级CNN模型（61.6万参数），发现在严重口吃事件（如阻塞、声音重复）发生前存在可捕获的韵律前兆信号，从而实现具有严重程度选择性的前瞻预测。</li>
<li>展示了模型出色的跨人群迁移能力，无需微调即可在不同数据集（如口吃儿童数据集）上实现较好的检测和预测效果。</li>
<li>实现了高效的端侧设备部署方案，支持CoreML、ONNX和TFLite等格式，极低的延迟和资源占用证明了其在实际设备上进行实时预测的可行性。</li>
</ul></li>
</ul>""",
    """<ul>
<li><strong>核心贡献总结：</strong>
<ul>
<li>提出了一种知识驱动的语音目标提取方法，专门用于处理电影音频中混杂的背景音效干扰问题。</li>
<li>引入“发音方式（manners of articulation）”作为特定的知识源，在语音帧中进行检测并构建为知识向量，借此增强了难以从复杂背景音中分离的短语音片段的提取效果。</li>
<li>在近期的声音解混音挑战赛（Sound Demixing Challenge）的电影音频源分离测试中，证明了结合发音特征知识的方案显著优于无知识注入的方法，特别是在处理被不明背景声掩盖的语音段时效果卓越。</li>
</ul></li>
</ul>""",
    """<ul>
<li><strong>核心贡献总结：</strong>
<ul>
<li>提出了一种基于长上下文目标说话人的音视频自动语音识别（AV-ASR）系统（BUT系统），有效解决了多人重叠对话场景下的转录难题。</li>
<li>创新性地结合视觉线索，将预训练的AV-HuBERT视觉特征融入NVIDIA Parakeet-v2 ASR模型，支持在单次解码过程中处理长音频记录。</li>
<li>引入了利用Qwen3.5-122B大语言模型估计转录文本主题相似度的方法，并结合层次聚类，成功实现了将参与者准确划分为不同对话组的任务。</li>
<li>在CHiME-9 MCoRec挑战赛中表现出色，显著超越官方基线，并在评估集中取得第二名（仅以微弱差距落后于第一名）。</li>
</ul></li>
</ul>""",
    """<ul>
<li><strong>核心贡献总结：</strong>
<ul>
<li>提出了LRS-VoxMM，一个新的“真实场景下（in-the-wild）”的音视频语音识别（AVSR）基准测试集。</li>
<li>该数据集源自包含多样化现实对话及人工转录的VoxMM数据集，并被预处理为LRS格式，以便无缝集成到现有的AVSR研究流程中。</li>
<li>提供了包含加性噪声、混响和带宽限制等扭曲处理的评估子集，以支持模型在恶劣声学退化环境下的鲁棒性测试。</li>
<li>实验表明该基准较传统的LRS3更具挑战性，并证实了当音频信号质量下降时，视觉信息对识别准确率的贡献会更加显著，从而推动了对恶劣条件下视觉辅助作用的深入研究。</li>
</ul></li>
</ul>""",
    """<ul>
<li><strong>核心贡献总结：</strong>
<ul>
<li>指出了当前计算生物声学系统过度依赖于针对人类语音的16 kHz预训练模型的局限性，该做法往往会丢失动物叫声中高频（甚至是超声波）的重要信息。</li>
<li>提出了一种多频带编码框架，将动物叫声的全频谱分解为不同频带的特征，并将其融合为一个统一的表示形式。</li>
<li>通过相似性分析证实了特定编码器能产生去相关的频带嵌入，融合后有效提升了不同声音类别的可分性。</li>
<li>在三个生物声学数据集上的广泛实验表明，融合表示方法的表现持续优于传统的基带及时间扩展基线模型，展现了多频带方法在全频谱动物声音编码方面的巨大潜力。</li>
</ul></li>
</ul>"""
]

pattern = re.compile(r'<div class="paper-summary">.*?</div>', re.DOTALL)
matches = pattern.findall(content)

if len(matches) != 12:
    print(f"Error: Found {len(matches)} summaries instead of 12.")
else:
    new_content = content
    for match, summary in zip(matches, summaries):
        # We replace the text, taking care to keep the class div
        replacement = f'<div class="paper-summary">\n{summary}\n</div>'
        new_content = new_content.replace(match, replacement, 1)

    with open("DEST_REPO/arxiv-daily/index.html", "w", encoding="utf-8") as f:
        f.write(new_content)
    print("Successfully updated 12 summaries.")
